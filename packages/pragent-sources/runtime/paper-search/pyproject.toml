[project]
name = "cybereinstein-paper-search-adapter"
version = "0.1.0"
description = "Compliance boundary for PRAgent paper discovery."
requires-python = ">=3.11,<3.15"
dependencies = [
  "mcp[cli]==1.29.1",
  "paper-search-mcp==0.1.4",
]

[tool.uv]
package = false
